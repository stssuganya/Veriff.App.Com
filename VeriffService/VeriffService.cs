using System;
using Veriff.Core.IServices;
using Veriff.Core.Model;

namespace VeriffService
{
    public class VeriffService : IVeriffService
    {

        #region Create Veriff Session
        public bool CreateVeriffSession(verification verification)
        {
            bool isSaved = false;
            return isSaved;
        }
        #endregion
    }
}
